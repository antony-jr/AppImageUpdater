language: cpp
compiler: gcc
sudo: require
dist: xenial


script:
  - echo "Nothing to Build!"

branches:
  except:
    - # Do not build tags that we create when we upload to GitHub Releases
    - /^(?i:continuous)/
